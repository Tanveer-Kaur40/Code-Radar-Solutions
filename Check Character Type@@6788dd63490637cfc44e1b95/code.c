#include <stdio.h>
int main() {
    char word;
    scanf("%c",&word);
    if(isalpha(word)){
        if(word = 'a','e','i','o','u'){
            printf("Vowel");
        }
        else{
            printf("Consonant");
        }
    else if(isdigit(word)){
        printf("Digit");
    }
    else{
        printf("Special Character");
    }
        
    }
    else if()
    return 0;
}